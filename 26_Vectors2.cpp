#include<bits/stdc++.h>
using namespace std;

int main() {
    /* Deletion in vector */
    // vector<int> v = {10,20,12,23};
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;
    // v.erase(v.begin()+1);
    // for (auto i : v) cout<<i<<" ";

    // vector<int> v = {10,20,30,40,50};
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;
    // v.erase(v.begin()+1, v.begin()+4); // or v.erase(v.begin()+1, v.end()-1);
    // for (auto i : v) cout<<i<<" ";
    
    /* Insertion in vector */
    // vector<int> v(2,50);    
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;
    // v.insert(v.end()-1, 42);
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;
    // v.insert(v.begin(), 10);
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;
    // v.insert(v.end(), 10);
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;
    // v.insert(v.begin()+2, 3, 25);
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;

    // Copying one vector to another
    // vector<int> v1 = {1,2,3,4,5};
    // cout<<"v1 = ";
    // for (auto i : v1) cout<<i<<" ";
    // cout<<endl;
    // vector<int> v2;
    // v2.insert(v2.begin(), v1.begin(), v1.end());
    // cout<<"v2 = ";
    // for (auto i : v2) cout<<i<<" ";
    // cout<<endl;

    /* Other methods */
    // vector<int> v = {1,2,3,4,5};
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;
    // cout<<v.size()<<endl; // returns size of a vector
    // v.pop_back(); // removes last element
    // for (auto i : v) cout<<i<<" ";
    // cout<<endl;
    // cout<<v.size()<<endl;

    // vector<int> v1 = {10,20};
    // vector<int> v2 = {30,40};
    // cout<<"v1 = ";
    // for (auto i : v1) cout<<i<<" ";
    // cout<<endl;
    // cout<<"v2 = ";
    // for (auto i : v2) cout<<i<<" ";
    // cout<<endl;
    // v1.swap(v2); // swaps the content of two vectors
    // cout<<"v1 = ";
    // for (auto i : v1) cout<<i<<" ";
    // cout<<endl;
    // cout<<"v2 = ";
    // for (auto i : v2) cout<<i<<" ";
    // cout<<endl;

    // vector<int> v = {5,10,15};
    // cout<<v.empty()<<endl; // returns 'true' if v is empty, otherwise 'false'
    // v.clear(); // erases entire vector
    // cout<<v.empty()<<endl;
    return 0;
}